import { MainCategoryOption } from './main-category-option';

describe('MainCategoryOption', () => {
  it('should create an instance', () => {
    expect(new MainCategoryOption()).toBeTruthy();
  });
});
