import { ProductSelectorOption } from './product-selector-option';

describe('ProductSelectorOption', () => {
  it('should create an instance', () => {
    expect(new ProductSelectorOption()).toBeTruthy();
  });
});
