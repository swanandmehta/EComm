import { StoreAddressConfig } from './store-address-config';

describe('StoreAddressConfig', () => {
  it('should create an instance', () => {
    expect(new StoreAddressConfig()).toBeTruthy();
  });
});
